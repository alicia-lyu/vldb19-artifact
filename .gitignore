# Regenerable outputs (figures + macros + per-run CSVs land in paper-ready/
# and results/; the two committed sample PDFs in paper-ready/ are exceptions
# tracked explicitly via `git add -f`).
*.csv
*.png
*.tex
*stderr.txt
.DS_Store
/results
/TRASH
__pycache__/
