# TODO(reproduction agent): define `paper-ready` target that pulls the docker
# images, runs the TPC-H experiments (Q3, Q5, Q10 and Invoice-extended
# variants) across the four approaches, and writes plots into paper-ready/.
